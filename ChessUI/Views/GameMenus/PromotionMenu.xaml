<UserControl x:Class="ChessUI.PromotionMenu"
             Style="{StaticResource MenuAnimationStyle}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400">

    <Border Width="350" Height="140" Style="{StaticResource MenuOuterBorderStyle}">
        <Border Style="{StaticResource MenuInnerBorderStyle}">
            <Grid RenderOptions.BitmapScalingMode="HighQuality">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="SELECT A PIECE"
                           HorizontalAlignment="Center" VerticalAlignment="Bottom"
                           FontSize="20" FontWeight="SemiBold" Foreground="White"/>

                <UniformGrid Grid.Row="1" Columns="4" Rows="1" Margin="10,0">
                    <Image x:Name="imgQueen" MouseDown="imgQueen_MouseDown" Style="{StaticResource PromotionPieceStyle}" Margin="5"/>
                    <Image x:Name="imgRook" MouseDown="imgRook_MouseDown" Style="{StaticResource PromotionPieceStyle}" Margin="5"/>
                    <Image x:Name="imgKnight" MouseDown="imgKnight_MouseDown" Style="{StaticResource PromotionPieceStyle}" Margin="5"/>
                    <Image x:Name="imgBishop" MouseDown="imgBishop_MouseDown" Style="{StaticResource PromotionPieceStyle}" Margin="5"/>
                </UniformGrid>
            </Grid>
        </Border>
    </Border>
</UserControl>