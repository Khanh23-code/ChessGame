<UserControl x:Class="ChessUI.Views.BoardMenu.CountDownView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">

    <UserControl.Resources>
        <Storyboard x:Key="TextPopAnim">
            <DoubleAnimation Storyboard.TargetName="TextScale"
                             Storyboard.TargetProperty="ScaleX"
                             From="0.0" To="1.0" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="1"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

            <DoubleAnimation Storyboard.TargetName="TextScale"
                             Storyboard.TargetProperty="ScaleY"
                             From="0.0" To="1.0" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="1"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

            <DoubleAnimation Storyboard.TargetName="CountText"
                             Storyboard.TargetProperty="Opacity"
                             From="0.0" To="1.0" Duration="0:0:0.2"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Border Background="{StaticResource FrostedGlassBrush}" CornerRadius="8"/>

        <TextBlock x:Name="CountText"
                   Text=""
                   FontSize="150"
                   FontWeight="Bold"
                   Foreground="#FFA274"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   RenderTransformOrigin="0.5, 0.5">
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="5" Opacity="0.6"/>
            </TextBlock.Effect>

            <TextBlock.RenderTransform>
                <ScaleTransform x:Name="TextScale" ScaleX="1" ScaleY="1"/>
            </TextBlock.RenderTransform>
        </TextBlock>
    </Grid>
</UserControl>