<UserControl x:Class="ChessUI.PauseMenu"
             Style="{StaticResource MenuAnimationStyle}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="350">

    <Border Width="300" Height="160" Style="{StaticResource MenuOuterBorderStyle}">
        <Border Style="{StaticResource MenuInnerBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="GAME PAUSED" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="Segoe UI"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="btnContinue" Click="btnContinue_Click" Content="CONTINUE"
                            Width="100" Height="40" Margin="0,0,10,0"
                            Style="{StaticResource GameMenuGrayButtonStyle}" />

                    <Button x:Name="btnRestart" Click="btnRestart_Click" Content="RESTART"
                            Width="100" Height="40"
                            Style="{StaticResource RestartGradientButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</UserControl>