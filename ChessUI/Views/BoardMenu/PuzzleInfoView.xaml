<UserControl x:Class="ChessUI.Views.BoardMenu.PuzzleInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Margin="20"
             Style="{StaticResource SlideInAnimation}"
             d:DesignHeight="700" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="GradientTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PlayGradientBrush}">
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Background="{StaticResource FrostedGlassBrush}">
    <Grid>
        <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">

            <Border x:Name="NormalHeader" Style="{StaticResource MenuInnerBorderStyle}" Padding="20" Visibility="Visible">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="PUZZLE RATING" Foreground="{StaticResource SmallTitleText}" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="1452" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,2,0,4"/>
                        <Border Background="#1500DC82" HorizontalAlignment="Left" CornerRadius="4" Padding="6,2">
                            <TextBlock Text="+8 today" Foreground="#00DC82" FontSize="11" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>

                    <Border Grid.Column="1" Width="56" Height="56" CornerRadius="28" Background="#353535" Margin="10,0,0,0">
                        <Grid>
                            <TextBlock Text="&#xE9D2;" FontFamily="Segoe Fluent Icons" FontSize="30" 
                                   Foreground="#FFA274" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <Border.Effect>
                            <DropShadowEffect Color="#FFA274" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                        </Border.Effect>
                    </Border>
                </Grid>
            </Border>

            <Border x:Name="DailyHeader" Style="{StaticResource MenuInnerBorderStyle}" Padding="20" Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <TextBlock Text="&#xE787;" FontFamily="Segoe Fluent Icons" Foreground="#AAAAAA" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="DAILY CHALLENGE" Foreground="{StaticResource SmallTitleText}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Border Grid.Row="1" BorderBrush="#30FFFFFF" BorderThickness="0,1,0,0" Padding="0,12,0,0">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock x:Name="txtDate" Text="Dec 20" Style="{StaticResource GradientTextStyle}" FontSize="42" FontWeight="Black" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="txtDayOfWeek" Text="Saturday" Foreground="White" Opacity="0.6" FontSize="14" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <StackPanel Grid.Row="1">
            <Border Background="#303030" CornerRadius="8" Padding="15,12" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="12" Height="12" Fill="White" Margin="0,0,12,0"/>
                    <TextBlock Text="Find the best move for White" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <WrapPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Border Background="#25FFFFFF" CornerRadius="12" Padding="14,6" Margin="0,0,8,8">
                    <TextBlock Text="Pin" Foreground="#E0E0E0" FontSize="13"/>
                </Border>
                <Border Background="#25FFFFFF" CornerRadius="12" Padding="14,6" Margin="0,0,8,8">
                    <TextBlock Text="Endgame" Foreground="#E0E0E0" FontSize="13"/>
                </Border>
                <Border Background="#25FFFFFF" CornerRadius="12" Padding="14,6" Margin="0,0,8,8">
                    <TextBlock Text="Mate in 2" Foreground="#E0E0E0" FontSize="13"/>
                </Border>
            </WrapPanel>

            <Border x:Name="FeedbackBox" Visibility="Collapsed" 
                Background="#2000DC82" BorderBrush="#00DC82" BorderThickness="1" CornerRadius="12" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Width="32" Height="32" CornerRadius="16" Background="#00DC82" Margin="0,0,15,0">
                        <TextBlock Text="&#xE73E;" FontFamily="Segoe Fluent Icons" Foreground="#11191F" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Success!" Foreground="#00DC82" FontWeight="Bold" FontSize="16"/>
                        <TextBlock Text="+12 Rating points" Foreground="#CCFFFFFF" FontSize="12" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
                <Border.Effect>
                    <DropShadowEffect Color="#00DC82" BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
                </Border.Effect>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Style="{StaticResource NeutralButton}" Height="48" FontSize="15">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE9A6;" FontFamily="Segoe Fluent Icons" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Hint" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="2" Style="{StaticResource NeutralButton}" Height="48" FontSize="15">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72C;" FontFamily="Segoe Fluent Icons" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Retry" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>

            <Button x:Name="ActionBtn" Style="{StaticResource StartButtonStyle}" Content="Next Puzzle" Height="52" FontSize="18"/>
        </StackPanel>
        </Grid>
    </Border>
    
</UserControl>